apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: home
  namespace: home
spec:
  entryPoints:
    - web
  routes:
  - match: Host(`192.168.1.22`) || Host(`home.local`)
    kind: Rule
    services:
    - name: home
      port: 80
