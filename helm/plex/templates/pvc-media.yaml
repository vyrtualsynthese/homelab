apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "nextcloud.plex.fullname" . }}-media
  labels:
  {{- include "nextcloud.plex.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: retain
  resources:
    requests:
      storage: 3Ti
