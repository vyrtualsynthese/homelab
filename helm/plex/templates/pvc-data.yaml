apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "nextcloud.plex.fullname" . }}-data
  labels:
  {{- include "nextcloud.plex.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: retain
  resources:
    requests:
      storage: 50Gi
