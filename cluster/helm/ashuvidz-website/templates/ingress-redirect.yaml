apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: ashuvidz-redirect
  namespace: ashuvidz
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`ashuvidz.com`)
      kind: Rule
      middlewares:
        - name: redirect-https
          namespace: kube-system
      services:
        - kind: TraefikService
          name: noop@internal
