apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: {{ include "linkstack.fullname" . }}-redirect-https
  labels:
    {{- include "linkstack.labels" . | nindent 4 }}
spec:
  redirectScheme:
    scheme: https
    permanent: true
